# Pre-flight CI gates — run on every push to main and every PR, BEFORE deploy.
# Mirrors `make -C backend pre-push` (see CLAUDE.md "CI gates"): gofmt, go vet,
# golangci-lint, swagger drift, race tests, binary build, frontend lint+build,
# and a gitleaks secrets scan.
name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - 'docs-site/**'
      - 'backend/docs/**'
      - '**/*.md'
      - 'mkdocs.yml'
      - '.github/workflows/deploy.yml'
      - '.github/workflows/docs.yml'
  pull_request:
    branches: [main]

permissions:
  contents: read

# One CI run per ref; newer pushes cancel stale in-flight runs.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: backend · gofmt/vet/lint/test/build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache-dependency-path: backend/go.sum
      - name: gofmt (must be empty)
        run: |
          files="$(gofmt -l .)"
          if [ -n "$files" ]; then echo "gofmt needed:"; echo "$files"; exit 1; fi
      - name: go vet
        run: go vet ./...
      - name: golangci-lint
        run: make ci-lint
      # swagger drift шалгалт public-gerege-core repo руу шилжсэн — handler-ийн
      # annotation-ууд тэнд байрлана. Энэ репо нимгэн апп тул docs/ байхгүй.
      - name: unit tests (race + coverage)
        run: make ci-test
      - name: build api binary
        run: make ci-build

  frontend:
    name: frontend · lint/build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run build

  secrets-scan:
    name: gitleaks
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: gitleaks (scan working tree)
        run: |
          set -euo pipefail
          # Pin the version instead of querying the GitHub API for "latest":
          # the unauthenticated api.github.com call is rate-limited per runner IP
          # and returned 403 intermittently, failing CI for infra reasons. Bump
          # this pin deliberately.
          ver="8.21.2"
          echo "gitleaks v$ver"
          curl -sSfL "https://github.com/gitleaks/gitleaks/releases/download/v${ver}/gitleaks_${ver}_linux_x64.tar.gz" \
            | tar -xz gitleaks
          ./gitleaks detect --no-git --source . --config .gitleaks.toml --redact --no-banner
